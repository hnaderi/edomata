Port Edomata to Rust exactly as specified in docs/plans/rust-port.md (read it fully first; it is the source of truth for scope, crates, translation rules, wire compatibility, broker distribution and quality bar). Work milestone by milestone in the order listed, each in its own worktree and PR. The goal is met only when ALL of these hold: (1) every milestone in docs/plans/rust-port.md has a merged or open PR; (2) in rust/, `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo test --workspace --all-features` pass with PostgreSQL from docker-compose running; (3) rust/PORTING.md maps all 17 Scala modules and every Scala test suite to Rust with no unported rows; (4) the golden DDL tests and the Scala/Rust cross-language compatibility test pass; (5) the Kafka and RabbitMQ relay integration tests pass; (6) `cargo tree` shows no JVM, Scala or Java dependency. Report failures truthfully; never weaken, skip or delete a test to reach the goal. Stop and ask if a requirement in the spec is contradictory or impossible.
