# Design goals

## Ideas

- Functional programming is cool!
- Message passing is key to reliable, resilient systems.
- Event-driven design is necessary for natural domain modelling.
- DDD is a great design guideline.
- State machines are the building blocks of distributed systems.

## To achieve

- Expressive domain modelling.
- Test-driven development and testing in general must be as easy as comparing literal values.
- Serve as a CQRS chassis.
- Run a given program with different interpreters that have different characteristics (in memory, PostgreSQL, ...).

## Rust-specific goals

- **Wire compatibility with the Scala library**: same tables, same DDL (checked byte for byte against golden files generated by the Scala code), same JSON payloads by default (`jsonb`), so both implementations can share a database. A cross-language test writes with Scala, reads and appends with Rust, and verifies with Scala.
- **Idiomatic Rust, not emulated Cats**: `async fn` and futures instead of `F[_]`, `Result<T, NonEmpty<R>>` instead of `ValidatedNec`, traits and generic associated types instead of typeclasses, `serde` as the single serializer.
- **No JVM anywhere**: every connector uses the standard Rust crate for the job (`sqlx`, `serde`, `rdkafka`, `lapin`); the dependency tree contains no Java or Scala artifact.
- **Safety**: `#![forbid(unsafe_code)]` in every crate, a declared MSRV checked in CI, every public item documented.

## Non goals

- General tools for all scenarios: this library is designed for event-driven / event-sourced applications with a DDD mindset; it suits those problems and is probably a bad fit for others.
- Performance above expressiveness: unnecessary work is eliminated where possible, but expressiveness has priority. Concrete performance improvements are welcome as issues or pull requests.
